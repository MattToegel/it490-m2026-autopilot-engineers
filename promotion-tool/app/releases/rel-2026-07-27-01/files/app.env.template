# app/.env.template  (App role)  -  ns87 (Noaman Shahid), Person B
# Part of bulk release rel-2026-07-27-01. NO real secrets - placeholders only.
# Each lane keeps its own real app/.env (not in Git, not promoted).

RABBITMQ_HOST=__SET_PER_LANE__
RABBITMQ_PORT=5672
RABBITMQ_USER=__SET_PER_LANE__
RABBITMQ_PASSWORD=__SET_PER_LANE__
RABBITMQ_VHOST=/
